<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
       /* Reset some default styles */
        body, h1, form, div, label, input, button, p, a {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Basic body styling */
        body {
            font-family: 'Helvetica Neue', sans-serif;
            background-color: #f0f0f0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        /* Center the form */
        form {
            background-color: #ffffff;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 320px;
            text-align: center;
        }

        /* Form title styling */
        h1 {
            margin-bottom: 25px;
            font-size: 28px;
            color: #333;
        }

        /* Input field styling */
        label {
            display: block;
            margin-bottom: 8px;
            color: #444;
            font-weight: 600;
            text-align: left;
        }

        input[type="text"],
        input[type="email"],
        input[type="password"] {
            width: calc(100% - 24px); /* Adjust width to account for padding */
            padding: 12px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }

        /* Button styling */
        button {
            width: calc(100% - 24px); /* Adjust width to account for padding */
            padding: 12px;
            background-color: #28a745;
            border: none;
            border-radius: 5px;
            color: white;
            font-size: 18px;
            cursor: pointer;
            margin-bottom: 15px;
        }

        button:hover {
            background-color: #218838;
        }

        /* Text and link styling */
        p {
            font-size: 14px;
            color: #555;
            margin-top: 15px; /* Add some spacing above the text */
        }

        a {
            color: #007bff;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }
        /* Error message styling */
        .error-message {
            color: red;
            margin-bottom: 20px;
            text-align: left;
            list-style-type: none;
            padding-left: 0;
        }
    </style>
</head>
<body>
    <form action="/users/register" method="post">
        <h1>Register</h1>
        <% if (typeof errors != 'undefined') { %>
            <% errors.forEach(error => { %>
            <ul class="error-message"><%= error.message %></ul>
        <% })} %>
        <div>
            <label for="Name"> Name </label>
            <input type="text" id="name" name="name" placeholder="please enter your Name" required>
        </div>
        <div>
            <label for="Email"> Email </label>
            <input type="email" id="email" name="email" placeholder="please enter your Email" required>
        </div>
        <div>
            <label for="password2"> Password </label>
            <input type="password" id="password" name="password" placeholder="please enter your Password" required>
        </div>
        <div>
            <label for="password"> Confirm Pssword </label>
            <input type="password" id="password2" name="password2" placeholder="Confirm Password" required>
        </div>
        <div>
            <button type="submit" value="register">Register</button>
        </div>
        <p> Already Registered ? ||  <a href="/users/login"> Login </a></p>
        
    </form>
    <!-- <script>

        window.onload = function() {
            // Clear errors after the page is loaded
            document.querySelector('.error-message').innerHTML = '';
        };
    </script> -->
</body>
</html>